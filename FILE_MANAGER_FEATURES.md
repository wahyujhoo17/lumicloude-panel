# File Manager Features

## ✨ Fitur yang Tersedia

### 1. **File Editor**

- ✅ Klik file untuk edit langsung di browser
- ✅ Support untuk semua text files: `.js`, `.html`, `.css`, `.php`, `.json`, `.txt`, `.md`, etc.
- ✅ Syntax-aware text editor dengan modal fullscreen
- ✅ Save langsung ke server

### 2. **ZIP File Extraction**

- ✅ Upload file ZIP
- ✅ Klik tombol Extract (🗂️) untuk unzip
- ✅ Otomatis extract ke folder dengan nama yang sama

### 3. **File Management**

- ✅ Upload files
- ✅ Download files
- ✅ Delete files
- ✅ Edit files inline
- ✅ Navigate folders

### 4. **Folder Management**

- ✅ Create new folder (tombol "New Folder")
- ✅ Navigate into folders
- ✅ Go back to parent folder

### 5. **Create New Files**

- ✅ Create new empty file (tombol "New File")
- ✅ Langsung bisa diedit setelah dibuat

## 🎯 Cara Menggunakan

### Edit File:

1. Klik nama file yang ingin diedit
2. Editor akan terbuka dalam modal
3. Edit konten file
4. Klik "Save" untuk menyimpan

### Extract ZIP:

1. Upload file ZIP
2. Klik icon Archive (🗂️) di sebelah file ZIP
3. Confirm extraction
4. File akan di-extract ke folder baru

### Create Folder:

1. Klik tombol "New Folder"
2. Masukkan nama folder
3. Klik "Create"

### Create File:

1. Klik tombol "New File"
2. Masukkan nama file (contoh: `index.html`)
3. Klik "Create"
4. File baru akan muncul dan bisa langsung diedit

## 📝 File Types yang Bisa Diedit

- `.txt` - Text files
- `.js`, `.jsx` - JavaScript
- `.ts`, `.tsx` - TypeScript
- `.json` - JSON config
- `.html` - HTML files
- `.css` - Stylesheets
- `.php` - PHP scripts
- `.py` - Python files
- `.md` - Markdown
- `.yml`, `.yaml` - YAML config
- `.xml` - XML files
- `.env` - Environment variables
- `.htaccess` - Apache config

## 🚀 Future Enhancements (Optional)

Jika ingin lebih advanced, bisa ditambahkan:

- Monaco Editor (VS Code editor) untuk syntax highlighting
- File preview untuk images
- Bulk operations (multi-select files)
- File search functionality
- File permissions management
- File rename functionality
- Drag & drop upload
